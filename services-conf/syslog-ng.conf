@version: 4.8

# HPCPerfStats pipeline syslog-ng. Remote source + allowlist filter are generated
# from hpcperfstats.ini into syslog-ng.d/10-hpcperfstats-generated.conf (see
# render_syslog_ng_generated).

# First, set some global options.
options {
      chain_hostnames(off);
      flush_lines(0);
      use_dns(no);
      use_fqdn(yes);
      dns_cache(yes);
      owner("hpcperfstats");
      group("hpcperfstats");
      perm(0644);
      stats(freq(60));
      keep_hostname(yes);
      ts-format(iso);
      frac-digits(8);
      trim-large-messages(yes);
      log-msg-size(1000000);
};

@include "/home/hpcperfstats/services-conf/syslog-ng.d/10-hpcperfstats-generated.conf"

########################
# Sources (local only)
########################
source s_src {
       system();
       internal();
};

########################
# Destinations
########################
destination d_hps_cluster_logs {
       file("/hpcperfstats/logs/current/$HOST.$R_YEAR$R_MONTH$R_DAY.log"
            create-dirs(yes));
};

destination d_console { usertty("root"); };
destination d_xconsole { pipe("/dev/xconsole"); };

########################
# Filters (unused legacy samples retained for reference)
########################
filter f_dbg { level(debug); };
filter f_info { level(info); };
filter f_notice { level(notice); };
filter f_warn { level(warn); };
filter f_err { level(err); };
filter f_crit { level(crit .. emerg); };

filter f_debug { level(debug) and not facility(auth, authpriv, news, mail); };
filter f_error { level(err .. emerg) ; };
filter f_messages { level(info,notice,warn) and
                    not facility(auth,authpriv,cron,daemon,mail,news); };

filter f_auth { facility(auth, authpriv) and not filter(f_debug); };
filter f_cron { facility(cron) and not filter(f_debug); };
filter f_daemon { facility(daemon) and not filter(f_debug); };
filter f_kern { facility(kern) and not filter(f_debug); };
filter f_lpr { facility(lpr) and not filter(f_debug); };
filter f_local { facility(local0, local1, local3, local4, local5,
                        local6, local7) and not filter(f_debug); };
filter f_mail { facility(mail) and not filter(f_debug); };
filter f_news { facility(news) and not filter(f_debug); };
filter f_syslog3 { not facility(auth, authpriv, mail) and not filter(f_debug); };
filter f_user { facility(user) and not filter(f_debug); };
filter f_uucp { facility(uucp) and not filter(f_debug); };

filter f_cnews { level(notice, err, crit) and facility(news); };
filter f_cother { level(debug, info, notice, warn) or facility(daemon, mail); };

filter f_ppp { facility(local2) and not filter(f_debug); };
filter f_console { level(warn .. emerg); };

# Remote cluster ingest: allowlist from ini, all severities (not warn-only).
filter f_hps_remote_levels { level(debug..emerg); };

########################
# Log paths
########################
log {
      source(s_net);
      filter(f_hps_syslog_allow_net);
      filter(f_hps_remote_levels);
      destination(d_hps_cluster_logs);
};

# Disable console logging to avoid sending any messages to root tty.
# log { source(s_src); filter(f_dbg); destination(d_console); };
