# Console logging for RabbitMQ (compose stdout).
# warning: surface memory/disk resource alarms without info-level connection/channel flood
# under thousands of monitor publishers. Keep connection/channel at error.
log.console = true
log.console.level = warning
log.connection.level = error
log.channel.level = error
