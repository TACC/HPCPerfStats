# Monitor payloads can exceed the RabbitMQ 3.8+ default (16 MiB). Observed rejects were
# ~41 MiB (precondition_failed on basic.publish). 64 MiB leaves headroom for growth.
max_message_size = 134217728
