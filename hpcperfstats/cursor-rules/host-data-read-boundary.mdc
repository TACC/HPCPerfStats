---
description: host_data is not a user-facing request-time source; use artifacts; exceptions for admin_monitor and host_plot
alwaysApply: false
globs: hpcperfstats/site/lib/machine/api.py,hpcperfstats/site/lib/machine/job_plot_artifacts.py,hpcperfstats/site/lib/machine/job_detail_artifacts.py,hpcperfstats/site/lib/machine/artifact_readiness_expressions.py
---

# host_data Read Boundary

**Activation:** `alwaysApply: false` — when triggers in `agent-discipline-core.mdc` match, **Read this file before editing**. Triggered rules are mandatory, not optional.

`host_data` is a raw telemetry source. Treat it as a script/monitor/admin data source, not a user-facing request-time source.

## Policy

- User-facing API handlers and frontend-serving assembly code must not query `host_data` directly.
- User-facing routes must read from derived cached artifacts produced by metrics/update pipelines (`metrics_data`, `job_plot_artifact`, `job_detail_artifact`, and related cache entries).
- Direct `host_data` reads are allowed only for:
  - ingest/sync scripts and metrics/update pipelines,
  - **staff** monitor paths (`job monitor`, `admin monitor`, and related monitor diagnostics),
    including Admin Monitor `?section=telemetry_health` (Redis
    ``recent_host:*``-scoped, host-batched 12h aggregate with
    ``host = ANY`` + ``SET LOCAL statement_timeout``, Django cache key
    ``KEY_ADMIN_TELEMETRY_HEALTH``, soft-fail on timeout or empty Redis
    inventory — **not** a site-wide ``GROUP BY type, event``),
  - **`host_plot`** (authenticated users; live `HostDataProvider` — rarely used; harden statement timeouts; do **not** convert to prewarm in the same task as Job Detail plots).

## Required implementation behavior

1. If a user-facing endpoint needs data currently coming from `host_data`, add/update an artifact prewarm path in `update_metrics` first.
2. Invalidate both Redis and DB-backed derived artifacts from ingest/sync invalidation hooks when raw telemetry changes.
3. User-facing endpoints must return deterministic `loading/not ready` responses when artifacts are missing; do not query `host_data` as runtime fallback.
4. Keep monitor/admin-only direct read paths explicit (auth-gated and documented in code).
5. **`job_plots` / Job Detail plots** are artifact-only (L1/L2). Fingerprint from persisted **`metrics_distinct_time_count`** only — never a live `host_data` distinct-time COUNT on the website. Null `plot_item` + `unavailable_reason` on a fingerprint-matching L2 row is **terminal unavailable**, not a miss.
6. **`job_detail`** must not construct `jid_table` for request assembly (use `job_data` / detail artifacts).

## Required tests

- Add or update API regression tests proving non-admin user-facing endpoints do not directly read `host_data` (except documented `host_plot`).
- Add/update pipeline tests showing `update_metrics` prewarms required artifacts.
- Add/update invalidation tests proving ingest/sync removes stale derived artifacts.
- Update web E2E tests when user-visible page behavior changes.

## Canonical files

- `hpcperfstats/site/lib/machine/api.py`
- `hpcperfstats/site/lib/machine/cache_utils.py`
- `hpcperfstats/site/lib/machine/job_plot_artifacts.py`
- `hpcperfstats/site/lib/machine/job_detail_artifacts.py`
- `hpcperfstats/site/lib/machine/artifact_readiness_expressions.py`
- `hpcperfstats/analysis/metrics/update_metrics.py`
- `hpcperfstats/dbload/sync_timedb.py`
