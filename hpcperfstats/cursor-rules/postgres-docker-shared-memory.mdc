---
description: PostgreSQL in Docker — /dev/shm "No space left on device" and shm_size tuning
globs:
  - "**/docker-compose*.yaml"
alwaysApply: false
---

# PostgreSQL Container Shared Memory (`/dev/shm`)

## What the error means

- Message like **`could not resize shared memory segment "/PostgreSQL.<digits>"`** with **`No space left on device`** refers to the **container’s tmpfs `/dev/shm`**, not necessarily host disk space on `/var/lib/postgresql/data`.
- PostgreSQL uses shared memory segments backed by **`shm_size`** on the **db** service in Compose.

## What to do

- Increase **`shm_size`** on the Timescale/Postgres service (e.g. in `docker-compose.yaml` under `db:` / dual-run `db_pg18:`) until heavy queries and parallel workers stop failing. Canonical compose today sets **`shm_size: "16gb"`** with **`shared_buffers=12GB`** on the `db` service (and the same shm on **`db_pg18`** during migrate) — keep README/`DEPLOY_CONCURRENCY_AND_NUMA.md` aligned when changing either. Smaller dev hosts may need a lower pair; document the tradeoff rather than silently shrinking production defaults.
- After changing **`shm_size`**, recreate the db container so the limit applies.

## Overlap

- **`postgres-custom-image-and-migrate-contract.mdc`** — homemade PG18 dual-run also keeps **`shm_size: "16gb"`**.
- Composes with **`local-compose-db-lifecycle-for-web-tests.mdc`** / **`full-test-with-db-redis.mdc`** when validating DB health after compose edits.
