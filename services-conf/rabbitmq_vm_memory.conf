# Cap RabbitMQ's own memory alarm at 96 GiB so publishers block instead of
# growing toward 40% of host RAM (the default relative watermark). Pair with
# compose mem_limit/memswap_limit 96g on the rabbitmq service. Recreate the
# rabbitmq service after changing this file.
vm_memory_high_watermark.absolute = 96GiB
