---
description: Startup checkpoint-driven async DAY_CLOSE for sync_timedb
alwaysApply: false
---

# sync_timedb startup day-close contract

When **`sync_startup_day_close_preflight=yes`** (default), the supervisor starts a **single-flight** background discover thread (`sync_timedb.py [thread:startup-day-close-preflight]`) in parallel with ingest. Ingest does **not** wait on completion.

## Canonical modules

| Concern | Location |
|---|---|
| Discover + submit | `hpcperfstats/dbload/sync_timedb_startup_day_close.py` → `StartupDayClosePreflight` |
| Async seal/raw/tar worker | `hpcperfstats/dbload/sync_timedb_async_day_close.py` → `AsyncDayCloseCoordinator` |
| Checkpoint subtract + classify | `load_checkpoint_path_set`, `build_unprocessed_raw_by_daily_tar`, `days_ingest_complete_by_checkpoint`, `daily_tar_eligible_for_day_close_submit`, `classify_day_close_candidates` in `sync_timedb_archive_helpers.py` |
| Persistence API | `sync_timedb_persistence.py` — checkpoint/manifest load/save via `load_persistence_document` / `save_persistence_document`; contract bump resets sidecars (`sync-timedb-persistence-contract.mdc`) |
| Manifest | `{archive_data_dir}/.sync_timedb_startup_day_close.json` |
| Checkpoint | `{archive_data_dir}/.sync_timedb_state.json` (envelope with `entries`; same path/size/mtime fingerprint as supervisor restart) |

## Eligibility (startup discover)

1. Wait for or derive **`StartupArchiveScanCoordinator`** snapshot (`sync-timedb-canonical-startup-archive-scan.mdc`); **no nested full collect** in `build_remaining_raw_stats_by_daily_gz`.
2. Subtract checkpoint paths on snapshot mapping (`build_unprocessed_raw_by_daily_tar(..., maintenance_snapshot=)`).
3. **Submit budget starts after scans** (`submit_t0` reset post unprocessed+remaining build).
4. Multi-slice discover until `pending_eligible` and `pending_retry` empty; manifest `phase=done` only then (not on SIGTERM with deferrals).
5. Transient disqualification skips enqueue `pending_retry`; next slice retries when `tar not in get_disqualified_daily_tars()`.
6. Slice 2+ refresh `remaining_raw_by_gz` from janitor accrual when present, else coordinator snapshot.
7. **One** `get_disqualified_daily_tars()` per discover slice (before submit loop); pass precomputed set into `submit_day_close(..., disqualified_daily_tars=)` to avoid a second full recompute under `async_day_close._lock`.
8. Slice telemetry: `discover_slice: disqualified_n=… disq_elapsed_s=…` and `discover slice: submitted=…` in `try/finally` (always emitted even when candidate report is slow).
9. For each checkpoint-complete day with work left, `submit_day_close(..., reason=startup_checkpoint_complete)`; log submit skips.

**Not** gated on global `pending_stats_files` length.

## INI knobs

- `sync_startup_day_close_preflight` (default yes)
- `sync_startup_day_close_budget_seconds` (default 300) — applies to **submit** loop after scans
- `sync_startup_day_close_days_per_slice` (default 1)
- `sync_startup_snapshot_wait_seconds` (default 300, min 120)
- `sync_startup_day_close_scan_budget_seconds` (optional warn-only)
- `sync_day_close_async_workers` (default 1)
- `sync_day_close_max_inflight` (default = `sync_day_close_async_workers`) — skip expensive discover scan and defer submits when `active_or_submitted_tar_paths()` ≥ cap
- `sync_startup_day_close_backoff_seconds` (default 30) — sleep between discover slices when last slice submitted 0 due to async saturation (replaces 1s tight loop)
- `sync_day_close_candidate_report` (default yes) — `queued` / `waiting_on_ingest` / `eligible_deferred` / `disqualified`; `skipped_no_work` silent

## Operator log (candidate report)

```
janitor: day_close candidate report reason=startup_checkpoint_discover queued=N waiting_on_ingest=W eligible_deferred=D disqualified=M
sync_timedb: startup day close discover backoff async saturated active=N max=M …
sync_timedb: startup day close discover: scans elapsed_s=…
sync_timedb: startup day close submit skip tar=… reason=…
```

## Coordination

- **`StartupTarSealPreflight`** must skip days with active/submitted async day-close (`has_async_day_close_for_tar`) to avoid double seal. See **`sync-timedb-startup-tar-seal-contract.mdc`**.
- Janitor **`DAY_CLOSE`** debt submits to the same coordinator (no sync zstd on janitor thread). See **`sync-timedb-archive-janitor-contract.mdc`**.

## Tests

- `hpcperfstats/tests/test_sync_timedb_startup_day_close.py`
- `hpcperfstats/tests/test_sync_timedb_supervisor.py` (`test_supervisor_starts_ingest_without_waiting_for_startup_day_close`)
- Archive/janitor/async helpers in `test_sync_timedb_archive.py`, `test_sync_timedb_janitor.py`

## Overlap

- **`sync-timedb-archive-janitor-contract.mdc`** — scheduled/immediate enqueue + async execution.
- **`sync-timedb-db-before-archive-contract.mdc`** — raw delete still DB-gated in `DayRawRemovalCoordinator`.
- **`process-title-and-pool-labels.mdc`** — `set_daemon_thread_title("", script_name=..., role=...)` on discover and async worker threads.
