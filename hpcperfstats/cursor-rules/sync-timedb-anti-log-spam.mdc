---
description: Hot wait/poll/retry/defer INFO must be rate-limited with suppressed_n= (never bare log_print in loops)
alwaysApply: false
globs: hpcperfstats/dbload/lib/sync_timedb_archive_members_redis.py,hpcperfstats/dbload/lib/sync_timedb_populate_pool.py,hpcperfstats/dbload/sync_timedb*.py,hpcperfstats/tests/test_sync_timedb_archive_members_redis.py
---

# sync_timedb Anti Log Spam

**Activation:** `alwaysApply: false` — when triggers in `agent-discipline-core.mdc` match, **Read this file before editing**. Triggered rules are mandatory, not optional.

## Law

INFO/WARN on **hot wait / poll / retry / defer** paths that can fire **per worker** or **per poll** must use **rate-limiting** and append **`suppressed_n=`** when prior emissions were suppressed. **Never** bare `log_print` / `logger.info` inside those loops.

Prefer **Redis `SET NX EX`** when N processes (ingest-pool / populate waiters / main) share the same calendar day or key — process-local intervals alone still emit ~N lines per window.

Canonical helper: `_rate_limited_day_info_log` (+ day-specific wrappers) in `sync_timedb_archive_members_redis.py`.

## Motivating regression (RC-ER, 2026-07-29)

Bare `log_print("INFO: populate empty recover deferred … reason=archive_append_inflight")` in `wait_for_complete_members` fired on every empty-recover trip while append was inflight. Concurrent ingest-pool waiters produced tens of identical INFO lines per second.

**Required:** `_log_empty_recover_deferred_if_allowed` (process-local + Redis NX). Sibling patterns already covered: `_log_append_inflight_defer_if_allowed`, `_log_identity_drift_if_allowed`, `_log_stale_incomplete_if_allowed`.

## Required tests (same task)

- `test_empty_recover_deferred_log_rate_limited`
- `test_empty_recover_deferred_log_rate_limited_cluster_wide`
- `test_empty_recover_deferred_while_archive_append_inflight` (asserts ≤1 INFO under many recover trips)
- Keep existing: `test_append_inflight_defer_log_rate_limited`, `test_identity_drift_rate_limited_per_day`, `test_stale_incomplete_log_rate_limited_cluster_wide`

## Overlap

- `sync-timedb-ingest-pool-io-coordination.mdc` — populate wait / append-inflight defer contracts; required-test list.
- `duplicate-threshold-helper-mandate.mdc` — three+ copy rate-limit blocks → shared `_rate_limited_day_info_log`.
- `compose-operator-terminal-commands.mdc` — do not ask operators for unfiltered pipeline firehose; rate-limited signatures keep greps usable.
- `docs/OPERATOR_SYNC_TIMEDB_STALL_VERIFY.md` — RC-ER: expect rate-limited `empty recover deferred` with optional `suppressed_n=`, not a continuous stream.
