---
description: Keep GPU job-detail metrics aligned with monitor host_data source precedence
alwaysApply: false
---

# GPU host_data source priority

**Activation:** `alwaysApply: false` — when triggers in `agent-discipline-core.mdc` match, **Read this file before editing**. Triggered rules are mandatory, not optional.

GPU-derived job-detail fields (including `gpu_count` and utilization summaries) must follow monitor-emitted `host_data.type` contracts and a deterministic source precedence.

## Source precedence contract

- Prefer `nvidia_gpu` when present; otherwise use `amd_gpu` for equivalent GPU summary fields.
- Do not merge NVIDIA and AMD streams into a mixed synthetic GPU type for a single field unless a feature explicitly requires mixed-vendor aggregation and tests are added.
- For newly added GPU summary statistics from monitor payloads, wire the same precedence into API assembly and UI rendering in the same task.

## Cross-layer consistency

- Keep Django API assembly (`hpcperfstats/site/machine/api.py`) and frontend consumers consistent for default/no-data behavior.
- Treat monitor `host_data.type` names as the contract. If monitor naming changes, update analysis/API precedence and tests together.
- When adding a new GPU summary field, also update user-facing definitions/docs in the same task.

## Required tests when behavior changes

- Extend API tests covering:
 - NVIDIA-only payloads
 - AMD-only payloads
 - both-present precedence behavior
 - no-GPU fallback behavior
- If UI behavior changes, update web page E2E tests as required by `web-pages-e2e-test-maintenance.mdc`.

## Overlap

- Complements `monitor-analysis-architecture-sync.mdc` (host_data type contracts) and `variable-metadata-monitor-contract.mdc` (counter/event naming contract).
